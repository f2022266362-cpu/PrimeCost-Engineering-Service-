// Single source of truth for the canonical domain.
// Change this one line if the live domain ever changes.
export const SITE_URL = 'https://www.primecost.biz';

export const isServer = typeof window === 'undefined';
